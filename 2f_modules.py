# Learn how to import module

import random # we enter this module to get random.randint function
for i in range(5):
    print(random.randint(1, 10)) 
    # random.randint function call evaluates random integer value between the range (1, 10)
    # randint is in the 'random' module
